
We use aws command line tool to upload dumped database files to amazon s3

reference: http://timkay.com/aws/


installation:
curl https://raw.github.com/timkay/aws/master/aws -o aws
chmod +x aws
cp aws /usr/bin

setup the aws key:
echo -e "xxx" >> /root/.awssecret
chmod 600 .awssecret